# START_PROMPT

How to use this file: put `RAGB_project_brief.md` and `CLAUDE.md` (both provided alongside this
file) into an empty project folder — nothing else needs to exist yet, no venv, no `git init`. Open
Claude Code in that folder and paste everything below the line as your first message.

---

Read `RAGB_project_brief.md` and `CLAUDE.md` in this directory in full. Then begin at Section 0a
(Environment Setup) and work through the Implementation Roadmap (Section 5) in order, one phase at a
time. After each phase, write a short phase report before moving on, and follow Section 14's
commit/push cadence as you go rather than batching everything to the end.

If you hit a genuinely ambiguous implementation-level decision, make the most reasonable call,
record it in the phase report, and keep going — don't stop to ask. Only stop for a scope-level
decision, or if Section 6a's fallback cascade is fully exhausted with no path forward.

Start now with Section 0a step 1.
