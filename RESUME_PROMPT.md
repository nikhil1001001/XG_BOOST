# RESUME_PROMPT

How to use this file: paste everything below the line as your first message in any Claude Code
session on this project after the first one — i.e. whenever you're picking the build back up after
a break, a restart, or a usage-limit reset.

---

This is a resumed session on the RAGB project. Before doing anything else: read `CLAUDE.md`, then
check `git log --oneline -20`, the most recent phase report, and whatever exists under
`results/tables/` and `logs/` to work out exactly which phase and step was last completed. State
that back to me in one or two sentences, then continue the Implementation Roadmap (Section 5 of
`RAGB_project_brief.md`) from there — don't restart or redo phases that are already done.
